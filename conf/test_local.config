/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run nf-core/phoenix -profile test,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

params {
    config_profile_name        = 'Test profile'
    config_profile_description = 'Minimal test dataset to check pipeline function'

    // Limit resources so that this can run on GitHub Actions
    max_cpus   = 2
    max_memory = '6.GB'
    max_time   = '6.h'

    fasta_href = '/home/allan/Documents/test-datasets/data/genomics/homo_sapiens/genome/chr21/sequence/genome.fasta' 
    bwa_index_href = '/home/allan/Documents/test-datasets/data/genomics/homo_sapiens/genome/chr21/sequence/' 

    fasta_mref = '/home/allan/Documents/test-datasets/mus_musculus/chr19/chr19.fasta' 
    bwa_index_mref = '/home/allan/Documents/test-datasets/mus_musculus/chr19/' 
}
